check process redis with pidfile {{ env.redis.pidfile }}
	start program = "/usr/local/sbin/redis-server {{ env.redis.configfile }}"
	stop  program = "/usr/bin/killall redis-server"
	if 5 restarts within 5 cycles then timeout
